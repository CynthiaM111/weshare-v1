# Copy to .env for local sandbox dev:
#   cp .env.example .env
#
# Copy to .env.production before a production EAS build (or set EAS secrets):
#   cp .env.production.example .env.production

# sandbox | production — controls test OTP hints, GPS dev bypass, etc.
EXPO_PUBLIC_APP_ENV=sandbox

# true = use ride from/to coords for start/complete (sandbox only; ignored in production)
EXPO_PUBLIC_GPS_DEV_MODE=true

EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_key

# --- Phone auth (Africa's Talking via Supabase Send SMS Hook — see supabase/config.toml) ---
# Enable Phone provider; hook URL → functions/v1/send-sms (not Twilio).
# AT sandbox: username "sandbox" + sandbox API key from africastalking.com
# Dev fallback: Supabase test numbers +250780000001–006, code 123456
